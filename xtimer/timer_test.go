package xtimer

import "testing"

func Test_handleTimeout(t *testing.T) {
	tests := []struct {
		name string
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			handleTimeout()
		})
	}
}
